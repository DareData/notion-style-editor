import type { ComponentStyleConfig } from '@chakra-ui/theme';

export const Tooltip: ComponentStyleConfig = {
  baseStyle: {
    borderRadius: '4px',
    padding: '4px 8px',
  },
  sizes: {},
  defaultProps: {},
};
